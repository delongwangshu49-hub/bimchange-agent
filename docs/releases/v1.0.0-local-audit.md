# 1.0.0 local preparation audit / 本地准备审计

This records the initial preparation before upload authorization. The subsequent source/documentation upload is authorized; historical no-write observations below describe that earlier preparation only. Binary distribution gates remain open.

本文记录上传授权之前的初始准备；后续源码/文档上传已获授权。以下“未写入”描述仅指此前准备阶段，二进制分发条件仍未闭环。

Date: 2026-09-12. State: source/document preparation verified; local validation binaries built; public binary distribution still blocked. No GitHub write or publication occurred.

日期：2026-09-12。状态：源码/文档准备已验证，本地验证二进制已构建，公开二进制分发仍阻断。未修改 GitHub、未发布。

## Verification performed / 已执行验证

| Check / 检查 | Result / 结果 |
|---|---|
| Full unittest discovery / 完整单测发现 | 62 discovered: 59 passed, 3 opt-in native tests skipped / 发现 62 项，59 通过，3 项原生 opt-in 跳过 |
| Native WebEngine suite / 原生三维套件 | The 3 skipped tests separately enabled: 3 passed / 单独启用上述 3 项，全部通过 |
| Complete R3 research / 完整 R3 研究 | 9 passed / 9 通过 |
| R4 synthetic research / R4 合成研究 | 2 passed / 2 通过 |
| Quickstart / 快速开始 | PASS; IFC4; 407 entities; one expected query result; 0 model calls / 通过，0 模型调用 |
| Release identity / 版本一致性 | Python/package/UI 1.0.0; built EXE FileVersion/ProductVersion 1.0.0; installer resource definition 1.0.0.0 / 均匹配 |
| README integrity / 完整性 | 43 relative targets exist; original animated logo SHA-256 unchanged / 43 个相对目标存在，Logo 动图摘要不变 |
| Public screenshots / 公开截图 | New program-generated synthetic IFC only; Chinese/English home and spatial views captured; home and Chinese spatial layout visually checked / 仅新生成合成 IFC，已截图并检查布局 |
| Frozen EXE / 冻结程序 | 3 actual scenes + 3 cached selections; latest selection only; same runtime; idle 600 ms adds 0 frames; session removed / 三场景及三次缓存选择，最新选择、运行时复用、静置零重绘、会话清理通过 |
| Git / 工作树 | Existing changes preserved; no staging/commit/tag/push / 保留既有改动，未暂存/提交/标签/推送 |

Total unique passing automated tests: 73 (= 59 + 3 + 9 + 2), excluding the additional quickstart and frozen application checks. This is local bounded evidence, not a compatibility or engineering certification. Initial WebEngine startup still produced a roughly 1.085 s maximum UI heartbeat gap on this synthetic smoke run; do not claim zero latency.

不重复累计自动化通过 73 项，另有 quickstart 和冻结程序验证。这是本机有界证据，不是兼容性或工程认证。本次合成烟雾中首次 WebEngine 启动仍产生约 1.085 秒最大界面心跳间隔，不宣称零延迟。

## Local validation artifacts — not public assets / 本地验证产物，非公开附件

| Artifact / 产物 | Bytes / 字节 | SHA-256 |
|---|---:|---|
| `BIMChange-Agent-1.0.0-win-x64-NOT-FOR-DISTRIBUTION.zip` | 254372576 | `481a4edb66e020d30025397d9b11986a763c1bff0bab4ec2eb4d5d775d3631dd` |
| `BIMChange-Agent-1.0.0-win-x64-setup-NOT-FOR-DISTRIBUTION.exe` | 162299439 | `7c5623586bba7f30404545675d129e75cdc2a43ec16a8cf622ca278a3b5569b6` |

Unpacked size: 628,212,158 bytes, within the 600 MiB gate. ZIP and installer are within 350/250 MiB gates. Installer is unsigned and was compiled, **not installed**. Stable AppId acceptance/upgrade is untested in this turn. Only the three IfcOpenShell runtime Pset schema IFC files occur in the portable package; public demo IFC files are not bundled.

解包体积通过 600 MiB 限制，ZIP/安装器通过 350/250 MiB 限制。安装器未签名，本轮仅编译，**未安装**；稳定 AppId 安装/升级尚未验收。便携包只含三份 IfcOpenShell 运行时 Pset Schema IFC，不随包放入公开演示 IFC。

Original README animation SHA-256: `f37b513d8519fafd82ac727ab11e13eae0d37db42f7231a9fe30c8dfd95e0ff5`. Historical v0.9.0 screenshots and Gate 4 graphics remain referenced. No frozen held-out input/result was read, rerun or modified during this preparation.

README 原动图摘要如上，历史 v0.9.0 截图和 Gate 4 图表仍有引用。本次准备不读取、重跑或修改冻结留出输入/结果。

## License investigation and remaining gates / 许可核查与剩余条件

The installed runtime reports Qt WebEngine 6.11.1 and Chromium 140.0.7339.225. The Addons wheel's direct license directory contains only the 470-byte `LicenseRef-Qt-Commercial.txt`; it is not a complete attribution inventory. An offline attempt to load the installed runtime's `chrome://credits` did not load successfully, so no complete embedded-credits extraction is claimed. The build still collects Quick3D-related DLLs via QML dependencies.

已安装运行时报告 WebEngine 6.11.1、Chromium 140.0.7339.225。Addons wheel 的直接许可目录仅含 470 字节商业许可引用，不能充当完整声明清单。离线读取内置 credits 页面未成功，不声称已提取完整材料。构建仍经 QML 依赖收集 Quick3D 相关 DLL。

The [public release checklist](v1.0.0-release-checklist.md) therefore remains open: audited/minimized exact dependency inventory; applicable notices and corresponding-source materials; a final rebuilt-and-tested distributable; isolated installer/upgrade acceptance; then maintainer publication authorization. No project relicensing, license purchase or source-supply commitment has been made.

因此[公开发布清单](v1.0.0-release-checklist.md)仍待完成：精确依赖清单审计/精简、适用声明与对应源码、最终重建及验证、隔离安装/升级验收，最后取得作者发布授权。不改项目许可、不购买许可、不作源码供应承诺。
