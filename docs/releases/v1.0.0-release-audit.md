# 1.0.0 final artifact audit / 最终产物审计

Date / 日期: 2026-09-12. This records exact artifacts, not general engineering or legal certification. / 本文记录准确产物，不构成一般工程或法律认证。

## Native runtime / 原生运行时

[Native build 34696249588](https://github.com/delongwangshu49-hub/bimchange-agent/actions/runs/34696249588) passed after restoring successful OCCT/Boost libraries without rebuilding them. IfcOpenShell base revision `1c5b825d8ef05ab9d14a15dac12e9eae2f5a37c2` has the documented SWIG declaration patch, with CGAL disabled. Eight IFC schemas and OpenCASCADE synthetic geometry passed; both CGAL kernel names were rejected.

原生构建复用了已成功的 OCCT/Boost，不重复编译；使用上述固定 IfcOpenShell 修订及已记录的 SWIG 声明补丁，禁用 CGAL。八个 IFC Schema、OpenCASCADE 合成几何验证通过，两种 CGAL 内核均拒绝。

Wheel SHA-256: `b1ea9a3373f344221e9824fb40e2b18681f966720cd968013ce5db8f7f2b20b3`. The wheel metadata is `0.8.5+nocgal.1`; its upstream runtime version string remains `0.8.0`. Build proof, the exact patch and original notices are retained in the package. / wheel 元数据为 `0.8.5+nocgal.1`，上游运行时版本字符串仍为 `0.8.0`；准确补丁、构建证明及原始声明随包保留。

## Product checks / 产品检查

- 88 unique tests passed with the rebuilt dependency: 74 regular, 11 R3/R4 research and 3 native WebEngine checks. / 新依赖下 88 项不重复测试通过：74 常规、11 R3/R4 研究、3 原生三维。
- Exact frozen executable: three synthetic scenes, three cached selections, latest-selection handling, worker/browser reuse and session removal passed; zero redraw frames during the measured 600 ms idle window. / 准确冻结 EXE 的三场景、三次缓存选择、最新选择、进程/浏览器复用及会话清理通过；实测 600 ms 静止窗口零重绘。
- Measured first scene 1.356 s; cached selections 0.194–0.266 s; first-start maximum GUI heartbeat gap 0.635 s. These are one local synthetic run, not a cross-machine benchmark or zero-stutter guarantee. / 首场景 1.356 秒、缓存选择 0.194–0.266 秒、首启最大界面心跳间隔 0.635 秒；仅为本机合成测试，不代表跨机器基准或绝不卡顿。
- No private building files, live model/API calls or reruns of the frozen held-out evaluation. Original logo GIF and historical research assets retained. / 不使用私人建筑文件、不调用在线模型/API、不重跑冻结留出评估，保留原始 Logo 动图与历史研究资源。

## Payload and source evidence / 产物与源码证据

Portable ZIP: 222,975,099 bytes; unpacked: 548,950,050 bytes. SHA-256: `51bf29c42c998f7aeb60019638979be1bf00a1e5b4ece628d0092c02f4f217bb`.

177 runtime PE files, including 19 Qt modules, were mapped to exact dependency evidence. All 91 supplied source archive hashes and 3,263 original notices were checked. Unused Quick3D/VirtualKeyboard/QML plugin collections are excluded. MIT remains the application source license; third-party components retain their own terms, notices and replacement/build instructions.

177 个运行时 PE（含 19 个 Qt 模块）已映射至精确依赖证据；核对 91 份源码归档摘要及 3,263 份原始声明。排除未用 Quick3D/VirtualKeyboard/QML 插件集合；应用源码保持 MIT，第三方组件保留其各自条款、声明及替换/构建说明。

The release attachments include `payload-manifest.json`, `license-review.json`, `source-inventory.json`, `frozen-smoke.json`, `installer-acceptance.json`, `release-evidence.json` and `SHA256SUMS.txt`. The verifier binds receipts to exact installer/portable bytes and verifies all inner source archives. / 发布附件以准确摘要绑定安装器、便携包、验收回执及全部内部源码归档。

## Installer acceptance / 安装器验收

[Acceptance run 34700346091](https://github.com/delongwangshu49-hub/bimchange-agent/actions/runs/34700346091) passed on a disposable Windows runner: fresh installation; upgrade from 0.9.0; all 1,117 payload file hashes; startup and offline comparison; shortcuts; preference preservation; uninstallation and registration removal; unchanged input files. The original failed upgrade found one obsolete Qt plugin; the corrected installer removes only that named bundled file, not a directory tree or user data.

隔离 Windows 验收全部通过：全新安装、从 0.9.0 升级、1,117 个产物文件摘要、启动与离线比较、快捷方式、偏好保留、卸载及注册信息清理、输入文件不变。首次升级发现的旧 Qt 插件现仅按指定文件清理，不递归删目录、不碰用户数据。

Accepted installer: 144,566,901 bytes; SHA-256 `4c076fd5b2fe021c88002b206af836eadd1609406efdff42b62a237484cd2dea`. The installed manifest SHA-256 is `4b5ffdb468810681534e77a522964046e4fbad79c16f97fca7d77f5899ca3a38`, identical to the frozen/audited portable payload. Temporary draft-access write permission was removed from the acceptance workflow after this run; the released workflow uses `contents: read` and can verify published assets.

上述安装器与准确 payload 摘要绑定，内容与冻结/审计便携包一致。验收后移除草稿读取所需的临时写权限；发布版本的工作流恢复 `contents: read`，可复验已公开附件。

Both artifacts are unsigned; SHA-256 establishes file identity, not publisher authentication. / 两种产物均未签名；SHA-256 确认文件一致性，不构成发布者身份认证。
