# Viewer third-party notice

The proof vendors exactly one pinned, unmodified Three.js distribution locally.
The vendored module, core, loader, controls, and loader utility files are from
Three.js r184 and are covered by the MIT license in
`vendor/THREE-LICENSE.txt`. A local import map resolves the addons' `three`
specifier without a package service or CDN.
